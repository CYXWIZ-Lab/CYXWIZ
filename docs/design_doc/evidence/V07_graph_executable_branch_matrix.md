# V-07 — Graph Executable Branch Matrix

## Fixture

- `fixture_id`:
- `graph_capable`:
- `date`:
- `environment`:

## Evidence fields

- `BuildExecutableFromConfig` branch decision
- emitted model class (`GraphExecutableModel` or sequential fallback)
- model artifact metadata
- fallback explanation and reproduction details

## Notes

- Populate with one graph-capable and one legacy-only run.
