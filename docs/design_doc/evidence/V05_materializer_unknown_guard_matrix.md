# V-05 — Materializer Unknown Guard Matrix

## Fixture

- `fixture_id`:
- `dataset_nodes`:
- `date`:
- `environment`:

## Evidence fields

- fixture classification table (Known / Unknown-legacy / invalid)
- resolved `materializer_source_kind`
- reason strings
- approval/denial outcome

## Notes

- Populate with matrix showing only explicitly approved branches yield `Unknown`.
