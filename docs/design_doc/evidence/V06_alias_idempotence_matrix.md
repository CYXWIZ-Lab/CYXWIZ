# V-06 — Alias Idempotence Matrix

## Fixture

- `fixture_id`:
- `alias_corpus`:
- `date`:
- `environment`:

## Evidence fields

- repeated compile input set
- canonical mapping snapshot (run 1)
- canonical mapping snapshot (run 2)
- duplicate/extra mapping diff
- comparison result (pass/fail)

## Notes

- Populate with proof of deterministic, single-pass canonicalization.
