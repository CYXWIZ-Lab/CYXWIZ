## 12) Critical invariants (must remain true)

1. No training starts without successful compile gate.
2. No training starts without preflight checks for selected mode.
3. Graph plan is immutable for a launch run.
4. Unsupported runtime mappings fail closed.
5. Core graph/editor abstractions should remain independent from plugin/extension internals.

---
